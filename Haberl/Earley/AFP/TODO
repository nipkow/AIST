Important
=========

Modularize bounds to simplify local code changes. Do not expand all T-bounds all the time.
Idea: if f uses g and h, express T_f bound only in terms of T_g and T_h (?)
(Of course at the end, everything needs to be expanded)

DERIVE O bounds directly using Lemma: f(n+1) = f n + g n ...
Can Lemma be generalized, eg
Lemma If f(n+1) = f(n) + h(n)*g1(n) + g2(n) (???)
  and h(n) <=0 n and g1(n) <=O n^k1 and g2(n) <=O n^k2
  then f(n) <=O h(n) * n^(k1+1) + n^(k2+1)  ???
Additional complication: terms like g(h...)
because we now need T_g(size(h(...))

Pre-computation:
T_Init_L(') is a hack.
Better: make Init_L_G a locale parameter
Earley_G_L = Earley_G
+ fixes Init_L_G assumes Init_L_G = Init_L ps S
Now Init_L_G needs no time

IL verification relates back to inductive Close2
but should use recursive close2L (May change complexity constants)

Layout: lines <= 100, esp for arithmetic formulas:
group, indent, break arithmetic formulas

Use new Array theory to allow easy code generation and to deal with
annoyances like xs @ [x] in function bins.

Closure (B,C): use lists directly, not sets?

Generic Array (type?) theory (Collection + syntax + lemmas)

Get rid of locales and replace with params?
+ time_fun works
+ can separate precomputed values more easily
- Defn of Earley etc depends on G and w

Allow epsilon. Aycock and Horspool: nullable + predictor.


Minor
=====

Item: get rid of rule index, use (A, rev \alpha, \beta, from).
(Improves constants

Complexity proofs: do not expand bounds of T_isin etc
but make upper bounds depend of them

Clean O proofs
what is w: 't list or sym list?
combine Predict and Complete?

Generic Closure2 worklist: inductive set -> inductive list -> while
Only relevant to obtain reusable component
